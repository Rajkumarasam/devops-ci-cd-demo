apiVersion: v1
kind: Service
metadata:
  name: devops-demo-service
  namespace: devops-demo
  labels:
    app: devops-demo-app
spec:
  type: ClusterIP
  selector:
    app: devops-demo-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
